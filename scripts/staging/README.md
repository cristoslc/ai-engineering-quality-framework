# Staging Exemption

This is a pure documentation and analysis repository. It has no runtime components, no deployable artifacts, and no server-side behavior. All validation is handled by:

- `mmdc` for Mermaid diagram verification
- markdownlint for prose quality

No staging target is needed.
